
1. LayerNorm
 Layer Normalization standardizes (Mean = 0, Std dev = 1)  the input features to a layer by using the mean and standard deviation calculated across the **features within a single training example (row)**. This process ensures that the normalization of a given input is independent of the other examples in the batch.



🤔 Thinking
1. What does it mean to normalize a matrix?